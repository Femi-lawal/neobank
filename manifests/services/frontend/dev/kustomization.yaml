apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: neobank-dev

resources:
  - ../../../base/frontend

commonLabels:
  environment: dev

# Image override - CI updates this via kustomize edit
images:
  - name: neobank/frontend
    newName: 123456789012.dkr.ecr.us-east-1.amazonaws.com/neobank/frontend
    newTag: latest

# Dev-specific patches
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: frontend

configMapGenerator:
  - name: frontend-config
    behavior: merge
    literals:
      - LOG_LEVEL=debug
      - ENVIRONMENT=dev
