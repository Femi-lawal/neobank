apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: neobank-dev

resources:
  - ../../../base/product-service

commonLabels:
  environment: dev

# Image override - CI updates this via kustomize edit
images:
  - name: neobank/product-service
    newName: 123456789012.dkr.ecr.us-east-1.amazonaws.com/neobank/product-service
    newTag: latest

# Dev-specific patches
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: product-service

configMapGenerator:
  - name: product-service-config
    behavior: merge
    literals:
      - LOG_LEVEL=debug
      - ENVIRONMENT=dev
