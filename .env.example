# =============================================================================
# NeoBank Local Development Environment Variables
# =============================================================================
# Copy this file to .env and customize as needed for local development.
# 
# IMPORTANT: .env is gitignored and should NEVER be committed to the repository.
# These are LOCAL DEVELOPMENT ONLY values - production uses AWS Secrets Manager.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_USER=neobank_dev
POSTGRES_PASSWORD=LocalDevPassword123!
POSTGRES_DB=newbank_core

# Shorthand aliases used by services
DB_HOST=postgres
DB_PORT=5432
DB_USER=neobank_dev
DB_PASSWORD=LocalDevPassword123!
DB_NAME=newbank_core

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# JWT secret for local development - NEVER use this value in production!
JWT_SECRET=local-dev-jwt-secret-change-in-production-environment

# -----------------------------------------------------------------------------
# Kafka Configuration (Local Development)
# -----------------------------------------------------------------------------
KAFKA_BROKERS=kafka:29092

# -----------------------------------------------------------------------------
# Service URLs (Internal Docker Network)
# -----------------------------------------------------------------------------
IDENTITY_SERVICE_URL=http://identity-service:8081
LEDGER_SERVICE_URL=http://ledger-service:8082
PAYMENT_SERVICE_URL=http://payment-service:8083
PRODUCT_SERVICE_URL=http://product-service:8084
CARD_SERVICE_URL=http://card-service:8085

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development

# Client-side API URLs (browser -> localhost)
NEXT_PUBLIC_IDENTITY_API=http://localhost:8081
NEXT_PUBLIC_LEDGER_API=http://localhost:8082
NEXT_PUBLIC_PAYMENT_API=http://localhost:8083
NEXT_PUBLIC_PRODUCT_API=http://localhost:8084
NEXT_PUBLIC_CARD_API=http://localhost:8085

# -----------------------------------------------------------------------------
# Optional: AWS Configuration (for testing AWS integrations locally)
# -----------------------------------------------------------------------------
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
