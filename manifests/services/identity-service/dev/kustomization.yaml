apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: neobank-dev

resources:
  - ../../../base/identity-service

commonLabels:
  environment: dev

# Image override - CI updates this via kustomize edit
images:
  - name: neobank/identity-service
    newName: 123456789012.dkr.ecr.us-east-1.amazonaws.com/neobank/identity-service
    newTag: latest

# Dev-specific patches
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: identity-service

configMapGenerator:
  - name: identity-service-config
    behavior: merge
    literals:
      - LOG_LEVEL=debug
      - ENVIRONMENT=dev
