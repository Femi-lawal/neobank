# =============================================================================
# AWS Dev Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values before deployment.
# These values are used with envsubst to substitute placeholders in YAML files.
#
# Usage:
#   export $(cat .env | xargs) && envsubst < kustomization.yaml > kustomization-rendered.yaml
#   kubectl apply -k .
# =============================================================================

# AWS Account Configuration
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# Database Configuration
# Get this from Terraform output: module.rds.endpoint
RDS_ENDPOINT=your-rds-instance.xxxxx.us-east-1.rds.amazonaws.com

# Redis Configuration  
# Get this from Terraform output: module.elasticache.primary_endpoint
REDIS_ENDPOINT=master.your-redis-cluster.xxxxx.use1.cache.amazonaws.com:6379

# =============================================================================
# IMPORTANT: Secrets Management
# =============================================================================
# DO NOT add secrets to this file. Instead use:
#
# 1. AWS Secrets Manager + External Secrets Operator (recommended)
# 2. Kubernetes Sealed Secrets
# 3. Manual kubectl create secret commands
#
# See the kustomization.yaml comments for detailed instructions.
# =============================================================================
